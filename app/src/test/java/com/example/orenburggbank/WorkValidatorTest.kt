/*
 * Портал техработ — АО «БАНК ОРЕНБУРГ»
 * Автор: Агайдаров Даулет Азаматович
 * Студент группы 23 КСК 4, Университетский колледж ОГУ
 * Производственная практика 2026
 */

package com.example.orenburggbank

import com.example.orenburggbank.data.entity.WorkEntity
import com.example.orenburggbank.domain.WorkValidator
import org.junit.Assert.*
import org.junit.Test

/**
 * Назначение: блок реализует отдельную функцию портала техработ.
 * Входные данные: параметры объявления.
 * Выходные данные: результат объявления или изменение состояния UI.
 * Роль в проекте: покрывает соответствующий сценарий технического задания.
 */
class WorkValidatorTest {
    /** Проверяет успешную валидацию полностью заполненной техработы из ТЗ п.1. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun validWorkHasNoErrors() {
        assertTrue(WorkValidator.validate(validWork(), setOf("abs")).isEmpty())
    }

    /** Проверяет обязательность названия, инженера, описания и плана отката. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun requiredFieldsAreValidated() {
        val errors = WorkValidator.validate(validWork().copy(title = "", engineerName = "", description = "", rollbackPlan = ""), setOf("abs"))
        assertEquals(4, errors.size)
    }

    /** Проверяет выбор затронутой системы только из справочника систем. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun systemMustComeFromList() {
        assertTrue(WorkValidator.validate(validWork().copy(affectedSystemIds = listOf("unknown")), setOf("abs")).any { "систем" in it.lowercase() })
    }

    /** Проверяет бизнес-правило: окончание техработы должно быть позже начала. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun endMustBeAfterStart() {
        assertTrue(WorkValidator.validate(validWork().copy(timeEnd = "09:00", endDateTime = "2026-06-15 09:00"), setOf("abs")).any { "позже" in it })
    }

    /** Проверяет допустимость завершения техработы на следующий календарный день. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun workMayEndNextDay() {
        assertTrue(WorkValidator.validate(validWork().copy(timeEnd = "01:00", endDateTime = "2026-06-16 01:00"), setOf("abs")).isEmpty())
    }

    /** Проверяет отклонение даты, не соответствующей формату хранения БД. */
    @Test
    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    fun malformedDateIsRejected() {
        assertTrue(WorkValidator.validate(validWork().copy(date = "15.06.2026", startDateTime = "15.06.2026 10:00"), setOf("abs")).any { "формату" in it })
    }

    /**
     * Назначение: блок реализует отдельную функцию портала техработ.
     * Входные данные: параметры объявления.
     * Выходные данные: результат объявления или изменение состояния UI.
     * Роль в проекте: покрывает соответствующий сценарий технического задания.
     */
    private fun validWork() = WorkEntity(0, "Работа", "2026-06-15", "10:00", "11:00", "abs", "Инженер", "Описание", "План отката", "pending")
}
